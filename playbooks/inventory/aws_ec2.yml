plugin: amazon.aws.aws_ec2
regions:
  - eu-central-1
filters:
  tag:Project: devops-infra
hostnames:
  - private-ip-address
keyed_groups:
  - key: tags.Type
    prefix: type
compose:
  ansible_connection: aws_ssm
